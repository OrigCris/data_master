[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
markers =
    integration: exige Spark local + Delta (roda no job de CI dedicado; auto-skip sem pyspark)
